# Changelog

All notable changes to the continuity plugin. Versions are deliberate SemVer —
see `RELEASING.md` in the fearsidhe-plugins marketplace repo. Format loosely
follows keepachangelog.com. The marketplace pin (released version) may lag
`main`; that gap is unreleased testing work.

## [Unreleased]
_Work merged since the last released pin lands here; roll into the next release._

## [0.0.1]
- Session-orientation `resume_brief` and `record_insight` MCP tools; vault read
  and write providers; memory write-bridge into the memory plugin.
